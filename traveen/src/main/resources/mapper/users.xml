<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.users.model.mapper.UsersMapper">

	<resultMap type="users" id="users">
		<result column="idx" property="idx"/>
		<result column="user_id" property="userId"/>
		<result column="user_pwd" property="userPwd"/>
		<result column="user_name" property="userName"/>
		<result column="email_id" property="emailId"/>
		<result column="email_domain" property="emailDomain"/>
		<result column="mobile" property="mobile"/>
		<result column="salt" property="salt"/>
		<result column="join_date" property="joinDate"/>
		<result column="grade" property="grade"/>
	</resultMap>
	
	
	<select id="idCheck" parameterType="String" resultType="int">
		select count(user_id)
		from users
		where user_id = #{userId}
	</select>

	<insert id="joinUsers" parameterType="users">
		insert into users (idx, user_id, user_pwd, user_name, email_id, email_domain, mobile, salt, join_date, grade)
		values (#{idx}, #{user_id}, #{user_pwd}, #{user_name}, #{email_id}, #{email_domain}, #{mobile}, #{salt}, now(), #{grade})
	</insert>

	<select id="loginUsers" parameterType="map" resultMap="users">
		select user_id, user_pwd
		from users
		where user_id = #{userId} and user_pwd = #{userPwd}
	</select>
</mapper>